{
  "name": "@odata-senado.cl/distiller-layer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "package": "rm -rf /tmp/distiller-layer && mkdir /tmp/distiller-layer && cp package.json /tmp/distiller-layer && cp -rL node_modules node_modules2 && rm -rf node_modules && mv node_modules2 node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-lambda-powertools/commons": "^2.10.0",
    "@aws-lambda-powertools/logger": "^2.8.0",
    "@aws-lambda-powertools/tracer": "^2.10.0",
    "axios": "^1.7.9",
    "cheerio": "^1.0.0"
  }
}
